rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write messages (for anonymous class board)
    match /messages/{messageId} {
      allow read, write: if true;
    }
  }
}
